export const getLengthErrorText = (length: number) =>
  `Must be at least ${length} characters long!`;
export const requiredFieldText = `It's required field!`;
export const validateEmailText = 'Must be validate email!';
